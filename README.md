## Password Manager API  
**Описание**  
RESTful API для управления паролями с полным набором CRUD операций.

---

### Эндпоинты

#### Получить все пароли  
- **Метод:** `GET`  
- **URL:** `/passwords`  
- **Описание:** Получить список всех сохранённых паролей  
- **Ответ:** `200 OK` — Массив объектов паролей  

#### Получить пароль по ID  
- **Метод:** `GET`  
- **URL:** `/passwords/:id`  
- **Параметры:**  
  - `id` (в пути) — ID пароля  
- **Ответы:**  
  - `200 OK` — Объект пароля  
  - `404 Not Found` — Пароль не найден  

#### Создать пароль  
- **Метод:** `POST`  
- **URL:** `/passwords`  
- **Тело:** Объект пароля (JSON)  
- **Ответы:**  
  - `201 Created` — Созданный пароль  
  - `400 Bad Request` — Неверные данные  

#### Обновить пароль  
- **Метод:** `PUT`  
- **URL:** `/passwords/:id`  
- **Параметры:**  
  - `id` (в пути) — ID пароля для обновления  
- **Тело:** Обновлённый объект пароля (JSON)  
- **Ответы:**  
  - `200 OK` — Обновлённый пароль  
  - `404 Not Found` — Пароль не найден  
  - `400 Bad Request` — Неверные данные  

#### Удалить пароль  
- **Метод:** `DELETE`  
- **URL:** `/passwords/:id`  
- **Параметры:**  
  - `id` (в пути) — ID пароля для удаления  
- **Ответы:**  
  - `204 No Content` — Успешно удалён  
  - `404 Not Found` — Пароль не найден  

---

### Примеры использования

```bash
curl -X POST http://localhost:3000/passwords \
  -H "Content-Type: application/json" \
  -d '{
    "service": "ВКонтакте",
    "category": "соцсеть",
    "length": 14
  }'
```

```bash
curl -X PUT http://localhost:3000/passwords/p123456 \
  -H "Content-Type: application/json" \
  -d '{
    "service": "VK",
    "category": "социальные сети",
    "password": "НовыйПароль123"
  }'
```

```bash
curl -X DELETE http://localhost:3000/passwords/p123456
```

```bash
curl -X GET http://localhost:3000/passwords
```

---

### Пример объекта пароля

```json
{
  "id": "p123456",
  "service": "ВКонтакте",
  "password": "aB3xK9mPzQ2w",
  "category": "c78901",
  "created": "2025-12-08 10:00:00"
}
```

---

> 💡 API автоматически генерирует надёжные пароли, управляет категориями и сохраняет все данные в зашифрованном виде.
