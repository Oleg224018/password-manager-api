# Password Manager API

RESTful API для управления паролями с полным набором CRUD операций.

## 🚀 Запуск

1. Убедитесь, что у вас установлен Go (версия 1.25 или новее).
2. Клонируйте репозиторий:
   ```bash
   git clone https://github.com/ваш-логин/password-manager-api.git
   cd password-manager-api
   ```

````

3. Установите зависимости:
   ```bash
   go mod tidy
   ```
4. Запустите сервер:
   ```bash
   go run main.go
   ```
   Сервер будет доступен по адресу `http://localhost:3000`.

## 📋 API Endpoints

### Получить все пароли

- **Метод:** `GET`
- **URL:** `/passwords`
- **Описание:** Получить список всех сохранённых паролей
- **Ответ:**
  - `200 OK` — Массив объектов паролей

### Получить пароль по ID

- **Метод:** `GET`
- **URL:** `/passwords/:id`
- **Параметры:** `id` (в пути) — ID пароля
- **Ответы:**
  - `200 OK` — Объект пароля
  - `404 Not Found` — Пароль не найден

### Создать новый пароль

- **Метод:** `POST`
- **URL:** `/passwords`
- **Тело:** JSON-объект с полями:
  - `service` (обязательное) — название сервиса (например, `"ВКонтакте"`)
  - `category` (опционально) — категория (по умолчанию `"другое"`)
  - `length` (опционально) — длина пароля (минимум 10, по умолчанию 10)
- **Ответы:**
  - `201 Created` — Созданный пароль
  - `400 Bad Request` — Неверные данные

Пример:

```bash
curl -X POST http://localhost:3000/passwords \
  -H "Content-Type: application/json" \
  -d '{
    "service": "ВКонтакте",
    "category": "соцсеть",
    "length": 14
  }'
```

### Обновить пароль

- **Метод:** `PUT`
- **URL:** `/passwords/:id`
- **Параметры:** `id` (в пути) — ID пароля для обновления
- **Тело:** JSON-объект с полями:
  - `service` — новое название сервиса
  - `category` — новая категория
  - `password` — новый пароль (если не указан — сгенерируется автоматически)
- **Ответы:**
  - `200 OK` — Обновлённый пароль
  - `404 Not Found` — Пароль не найден
  - `400 Bad Request` — Неверные данные

Пример:

```bash
curl -X PUT http://localhost:3000/passwords/p123456 \
  -H "Content-Type: application/json" \
  -d '{
    "service": "VK",
    "category": "социальные сети",
    "password": "НовыйПароль123"
  }'
```

### Удалить пароль

- **Метод:** `DELETE`
- **URL:** `/passwords/:id`
- **Параметры:** `id` (в пути) — ID пароля для удаления
- **Ответы:**
  - `204 No Content` — Успешно удалён
  - `404 Not Found` — Пароль не найден

Пример:

```bash
curl -X DELETE http://localhost:3000/passwords/p123456
```

### Дополнительные эндпоинты

#### Проверка работоспособности

- **Метод:** `GET`
- **URL:** `/health`
- **Ответ:**
  - `200 OK` — `{ "status": "ok", "time": "..." }`

#### Получить все категории

- **Метод:** `GET`
- **URL:** `/categories`
- **Ответ:**
  - `200 OK` — Массив категорий

## 🛡️ Безопасность

- Все данные хранятся в **зашифрованном файле** `passwords.json.encrypted`.
- Шифрование выполнено с использованием:
  - **scrypt** для генерации ключа из мастер-пароля
  - **AES-GCM** для шифрования данных
- Мастер-пароль задаётся в коде (для продакшена рекомендуется использовать переменные окружения).

## 📄 Пример объекта пароля

```json
{
  "id": "p123456",
  "service": "ВКонтакте",
  "password": "aB3xK9mPzQ2w",
  "category": "c78901",
  "created": "2025-12-08 10:00:00"
}
```

## 📄 Пример объекта категории

```json
{
  "id": "c78901",
  "name": "соцсеть"
}
```

## 📌 Примечания

- Этот API является RESTful-адаптацией консольного пароль-менеджера.
- Файл `passwords.json.encrypted` создаётся автоматически при первом запуске.
- Для корректной работы с русским языком в PowerShell рекомендуется использовать `curl.exe` или `Invoke-WebRequest` с явной кодировкой UTF-8.

## 🤝 Автор

Oleg (IS-323)

## 📜 Лицензия

MIT

```

---

### ✅ Что сделано:

- Полное соответствие структуре и стилю `fiber-college-done`.
- Все эндпоинты описаны на русском языке.
- Примеры запросов с русскими названиями (`"ВКонтакте"`, `"соцсеть"`).
- Указаны особенности безопасности (шифрование).
- Есть примеры JSON и примечания для Windows.

Теперь вы можете сохранить этот текст в файл `README.md` в корне проекта и загрузить всё на GitHub. Удачи! 🚀
```
````
